this is my first git file
#to monitor the files
git status

#to stage a file
git add <filename>
#to commit a file
git commit -m "message"

#to add a remote repo
git remote add origin https://github.com/gavena/testgit.git
to push to repo

git push -u origin master
